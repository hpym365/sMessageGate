<person>
	<student th:each="person : ${result}" >
		<name th:text="${person.name}"></name>
		<age th:text="${person.age}"></age>
		<devname th:text="${person.devname}"></devname>
		<xeguid th:text="${person.xeguid}"></xeguid>
	</student>
</person>